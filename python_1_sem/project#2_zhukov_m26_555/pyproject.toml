[project]
name = "yes"
version = "0.1.0"
description = ""
authors = [
    { name = "nikita", email = "dayzwaitik123@yandex.ru" }
]
requires-python = ">=3.10"
dependencies = []

[tool.poetry]
packages = [
    { include = "src" },
]

[tool.poetry.scripts]
project = "src.primitive_db.main:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"